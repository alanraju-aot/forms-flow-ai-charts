{{- if .Values.ingress.enabled }}
apiVersion: {{ include "common.capabilities.ingress.apiVersion" . }}
kind: Ingress
metadata:
  name: {{ include "ehr-connector.fullname" . }}-ingress
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "ehr-connector.labels" . | nindent 4 }}
  {{- if or .Values.ingress.annotations .Values.commonAnnotations }}
  {{- $annotations := include "common.tplvalues.merge" ( dict "values" ( list .Values.ingress.annotations .Values.commonAnnotations ) "context" . ) }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" $annotations "context" $) | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.ingress.ingressClassName (eq "true" (include "common.ingress.supportsIngressClassname" .)) }}
  ingressClassName: {{ .Values.ingress.ingressClassName | quote }}
  {{- end }}
  rules:
    {{- if .Values.ingress.hostname }}
    - host: {{ (tpl .Values.ingress.hostname .) | quote }}
      http:
        paths:
          {{- if .Values.ingress.extraPaths }}
          {{- toYaml .Values.ingress.extraPaths | nindent 10 }}
          {{- end }}
          - path: {{ .Values.ingress.path | default "/epic/.*" | quote }}
            {{- if eq "true" (include "common.ingress.supportsPathType" .) }}
            pathType: {{ .Values.ingress.pathType | default "Prefix" }}
            {{- end }}
            backend:
              service:
                name: {{ include "ehr-connector.serviceName" . }}
                port:
                  number: {{ .Values.service.port | default 8002 }}
    {{- end }}
    {{- range .Values.ingress.extraHosts }}
    - host: {{ (tpl .name $) | quote }}
      http:
        paths:
          - path: {{ default "/epic/.*" .path | quote }}
            {{- if eq "true" (include "common.ingress.supportsPathType" $) }}
            pathType: {{ default "Prefix" .pathType }}
            {{- end }}
            backend:
              service:
                name: {{ include "ehr-connector.serviceName" $ }}
                port:
                  number: {{ $.Values.service.port | default 8002 }}
    {{- end }}
    {{- if .Values.ingress.extraRules }}
    {{- include "common.tplvalues.render" (dict "value" .Values.ingress.extraRules "context" $) | nindent 4 }}
    {{- end }}
  {{- if or (and .Values.ingress.tls (or (include "common.ingress.certManagerRequest" ( dict "annotations" .Values.ingress.annotations )) .Values.ingress.selfSigned .Values.ingress.secrets )) .Values.ingress.extraTls }}
  tls:
    {{- if and .Values.ingress.tls (or (include "common.ingress.certManagerRequest" ( dict "annotations" .Values.ingress.annotations )) .Values.ingress.secrets .Values.ingress.selfSigned) }}
    - hosts:
        - {{ (tpl .Values.ingress.hostname .) | quote }}
      secretName: {{ default (printf "%s-tls" (tpl .Values.ingress.hostname .)) .Values.ingress.tlsSecretName }}
    {{- end }}
    {{- if .Values.ingress.extraTls }}
    {{- include "common.tplvalues.render" (dict "value" .Values.ingress.extraTls "context" $) | nindent 4 }}
    {{- end }}
  {{- end }}
{{- end }}
